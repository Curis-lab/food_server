import { AdminInteractor } from "../../../../src/use-cases/admin/admin.interactor";

// const interactor = new AdminInteractor("");
const mockRequest: { id: string } = {
  id: "1",
};
const mockResponse = {};

describe("get users", () => {
  it("should get user by id", () => {
    // interactor.searchVendorById(mockRequest, mockResponse);
  });
});
